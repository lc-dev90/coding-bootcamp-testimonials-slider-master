const imagens = [
  { nome: "image-john-jpg", key: 0 },
  { nome: "image-tanya-jpg", key: 1 },
];

document.querySelector("body").addEventListener("click", (e) => {
  console.log(e.target.classList.contains("left"));
  if(e.target.classList.contains("left")){

  };
});

function mudaFoto() {
  
}
